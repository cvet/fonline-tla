namespace Messaging
{

///@ Setting Common int32 Dialog.WhisperDist

///@ Enum MessageBoxType Look
///@ Enum MessageBoxType Talk
///@ Enum MessageBoxType Combat

///@ Enum SayType Normal
///@ Enum SayType Shout
///@ Enum SayType Whisper
///@ Enum SayType Emote
///@ Enum SayType Radio

///@ RemoteCall Server ReceiveChosenSay(SayType sayType, string text)
///@ RemoteCall Client ReceiveInfo(string text, TextPackKey textKey, string textArgs)
///@ RemoteCall Client ReceiveSay(ident crId, SayType sayType, bool onHeadOnly, string text, TextPackKey textKey, string textArgs)

///@ Property Player Server string LastSay Mutable
///@ Property Player Server int32 LastSayEqualCount Mutable

[[ModuleInit]]
void InitMessaging()
{
#if SERVER
#endif

#if CLIENT
    Game.OnConsoleMessage.Subscribe(OnConsoleMessage);
#endif
}

#if SERVER

void Info(Player player, string text)
{
    if (!text.isEmpty()) {
        player.ClientCall.ReceiveInfo(text, TextPackKey(), "");
    }
}

void Info(Player player, TextPackKey textKey, string textArgs = "")
{
    player.ClientCall.ReceiveInfo("", textKey, textArgs);
}

void Info(Critter cr, string text)
{
    if (!text.isEmpty()) {
        cr.PlayerClientCall.ReceiveInfo(text, TextPackKey(), "");
    }
}

void Info(Critter cr, TextPackKey textKey, string textArgs = "")
{
    cr.PlayerClientCall.ReceiveInfo("", textKey, textArgs);
}

void Say(Critter cr, string text)
{
    if (!text.isEmpty()) {
        SayExt(cr, SayType::Normal, false, text, TextPackKey(), "");
    }
}

void Say(Critter cr, TextPackKey textKey, string textArgs = "")
{
    SayExt(cr, SayType::Normal, false, "", textKey, textArgs);
}

void SayWhisper(Critter cr, string text)
{
    if (!text.isEmpty()) {
        SayExt(cr, SayType::Whisper, false, text, TextPackKey(), "");
    }
}

void SayWhisper(Critter cr, TextPackKey textKey, string textArgs = "")
{
    SayExt(cr, SayType::Whisper, false, "", textKey, textArgs);
}

void SayShout(Critter cr, string text)
{
    if (!text.isEmpty()) {
        SayExt(cr, SayType::Shout, false, text, TextPackKey(), "");
    }
}

void SayShout(Critter cr, TextPackKey textKey, string textArgs = "")
{
    SayExt(cr, SayType::Shout, false, "", textKey, textArgs);
}

void SayEmote(Critter cr, string text)
{
    if (!text.isEmpty()) {
        SayExt(cr, SayType::Emote, false, text, TextPackKey(), "");
    }
}

void SayEmote(Critter cr, TextPackKey textKey, string textArgs = "")
{
    SayExt(cr, SayType::Emote, false, "", textKey, textArgs);
}

void SayOnHead(Critter cr, string text)
{
    if (!text.isEmpty()) {
        SayExt(cr, SayType::Normal, true, text, TextPackKey(), "");
    }
}

void SayOnHead(Critter cr, TextPackKey textKey, string textArgs = "")
{
    SayExt(cr, SayType::Normal, true, "", textKey, textArgs);
}

void SayWhisperOnHead(Critter cr, string text)
{
    if (!text.isEmpty()) {
        SayExt(cr, SayType::Whisper, true, text, TextPackKey(), "");
    }
}

void SayWhisperOnHead(Critter cr, TextPackKey textKey, string textArgs = "")
{
    SayExt(cr, SayType::Whisper, true, "", textKey, textArgs);
}

void SayShoutOnHead(Critter cr, string text)
{
    if (!text.isEmpty()) {
        SayExt(cr, SayType::Shout, true, text, TextPackKey(), "");
    }
}

void SayShoutOnHead(Critter cr, TextPackKey textKey, string textArgs = "")
{
    SayExt(cr, SayType::Shout, true, "", textKey, textArgs);
}

void SayEmoteOnHead(Critter cr, string text)
{
    if (!text.isEmpty()) {
        SayExt(cr, SayType::Emote, true, text, TextPackKey(), "");
    }
}

void SayEmoteOnHead(Critter cr, TextPackKey textKey, string textArgs = "")
{
    SayExt(cr, SayType::Emote, true, "", textKey, textArgs);
}

[[ServerRemoteCall]] [[Async]]
void ReceiveChosenSay(Player player, SayType sayType, string text)
{
    text = text.trim();

    if (text.isEmpty()) {
        return;
    }

    // Todo: cleanup text from bad symbols
    // Keyb.RemoveInvalidChars(str, KIF_NO_SPEC_SYMBOLS);

    if (text == player.LastSay) {
        player.LastSayEqualCount += 1;

        if (player.LastSayEqualCount >= 5) {
            Game.Log("Player " + player.Name + " send same text more than 5 times. Disconnect");
            player.Disconnect();
            return;
        }
    }
    else {
        player.LastSay = text;
        player.LastSayEqualCount = 0;
    }

    Critter? cr = player.GetControlledCritter();
    if (cr == null || !Sync::LockCritterWithMap(cr)) {
        return;
    }

    SayExt(cr, sayType, false, text, TextPackKey(), "");
}

void SayExt(Critter cr, SayType sayType, bool onHeadOnly, string text, TextPackKey textKey, string textArgs)
{
    if (cr.IsDead()) {
        return;
    }

    Critter[] receivers;
    Map? map = cr.GetMap();

    if (map != null) {
        if (sayType == SayType::Shout) {
            receivers = map.GetCritters(CritterFindType::Players);
        }
        else if (sayType == SayType::Whisper) {
            receivers = map.GetCrittersInRadius(cr.Hex, Settings.Dialog.WhisperDist, CritterFindType::Players);
        }
        else {
            receivers = cr.GetCritters(CritterSeeType::WhoSeeMe, CritterFindType::Players);
            receivers.insertLast(cr);
        }
    }
    else {
        receivers = cr.GetCritters(CritterSeeType::WhoSeeMe, CritterFindType::Players);
    }

    for (int i = 0; i < receivers.length(); i++) {
        receivers[i].PlayerClientCall.ReceiveSay(cr.Id, sayType, onHeadOnly, text, textKey, textArgs);
    }
}

#endif

#if CLIENT

dict<MessageBoxType, ucolor> TextColors = {{MessageBoxType::Default, Color::Text},
                                           {MessageBoxType::Look, Color::DGreen},
                                           {MessageBoxType::Talk, Color::DGreen},
                                           {MessageBoxType::Combat, Color::DRed}};

ucolor GetTextColor(MessageBoxType type)
{
    return TextColors.exists(type) ? TextColors[type] : Color::Text;
}

void Info(MessageBoxType type, string text)
{
    Gui::MessageBox[] messageBoxes = Gui::CollectMessageBoxes();
    ucolor color = GetTextColor(type);

    text = Color::Tag(color) + Game.EncodeUtf8(0x2022) + " " + Color::Tag(Color::Text) + text;

    for (int i = 0; i < messageBoxes.length(); i++) {
        messageBoxes[i].AddMessage(text, type);
    }
}

void Info(string text)
{
    Info(MessageBoxType::Default, text);
}

void Info(TextPackKey textKey, string textArgs = "")
{
    Info(MessageBoxType::Default, textKey, textArgs);
}

void Info(MessageBoxType type, TextPackKey textKey, string textArgs = "")
{
    if (!Game.IsTextPresent(textKey)) {
        return;
    }

    string text = Game.GetText(textKey);
    text = Game.FormatTags(text, textArgs);

    Info(type, text);
}

[[Event]]
void OnConsoleMessage(string text)
{
    text = text.trim();

    if (text.isEmpty()) {
        return;
    }

    if (text[0] == "~") {
        text = text.substr(1).trim();

        if (!text.isEmpty()) {
            Info("Not implemented"); // Todo: restore admin commands
        }
    }
    else {
        SendSay(text);
    }
}

void SendSay(string text)
{
    SayType sayType = SayType::Normal;

    if (Input::IsShiftDown()) {
        sayType = SayType::Shout;
    }
    else if (Input::IsCtrlDown()) {
        sayType = SayType::Whisper;
    }

    if (sayType == SayType::Normal) {
        if (text.length() >= 2 && (text[0] == "/" || text[0] == ".")) {
            string ch = text[1];

            if (ch == "к" || ch == "К" || ch == "s" || ch == "S") {
                sayType = SayType::Shout;
            }
            else if (ch == "ш" || ch == "Ш" || ch == "w" || ch == "W") {
                sayType = SayType::Whisper;
            }
            else if (ch == "э" || ch == "Э" || ch == "e" || ch == "E") {
                sayType = SayType::Emote;
            }
            else if (ch == "р" || ch == "Р" || ch == "r" || ch == "R") {
                text = text.substr(2).trim();
                if (!text.isEmpty()) {
                    Radio::SendRadioMessage(text);
                }

                return;
            }

            if (sayType != SayType::Normal) {
                text = text.substr(2);
            }
        }
        else if (text.length() >= 4 && text[0] == "*" && text[1] != "*" && text[text.length() - 2] != "*" &&
                 text[text.length() - 1] == "*") { // RegExp: [*]([^*].*[^*])[*]
            sayType = SayType::Emote;
            text = text.substr(1, text.length() - 2);
        }
    }

    text = text.trim();

    if (!text.isEmpty()) {
        CurPlayer.ServerCall.ReceiveChosenSay(sayType, text);
    }
}

void LocalSay(Critter? cr, SayType sayType, bool onHeadOnly, string text, TextPackKey textKey, string textArgs)
{
    int formatCrNum = 0;
    int formatMbNum = 0;

    switch (sayType) {
    case SayType::Normal:
        formatMbNum = MsgStr::STR_MBNORM;
        formatCrNum = MsgStr::STR_CRNORM;
        break;
    case SayType::Shout:
        formatMbNum = MsgStr::STR_MBSHOUT;
        formatCrNum = MsgStr::STR_CRSHOUT;
        text = text.upper();
        break;
    case SayType::Whisper:
        formatMbNum = MsgStr::STR_MBWHISP;
        formatCrNum = MsgStr::STR_CRWHISP;
        text = text.lower();
        break;
    case SayType::Emote:
        formatMbNum = MsgStr::STR_MBEMOTE;
        formatCrNum = MsgStr::STR_CREMOTE;
        break;
    default:
        break;
    }

    if (onHeadOnly) {
        formatMbNum = 0;
    }

    if (text.isEmpty()) {
        if (!Game.IsTextPresent(textKey)) {
            return;
        }

        text = Game.GetText(textKey);
        text = Game.FormatTags(text, textArgs);
    }

    if (formatCrNum != 0 && cr != null) {
        cr.TextOnHead = FormatText(formatCrNum, text);
        cr.TextOnHeadEndTime = Game.FrameTime + Time::Milliseconds(Settings.Gui.TextDelay + text.length() * 100);
    }

    if (formatMbNum != 0) {
        string crName = cr != null ? cr.Name : "?";
        Info(MessageBoxType::Default, FormatText(formatMbNum, text, crName));
    }

    Game.FlashUnfocusedWindow();
}

string FormatText(int formatStrNum, string text1, string text2 = "")
{
    string formatStr = Game.GetText(TextPackKey(TextPackName::Game, "" + (formatStrNum)));
    return formatStr.replace("%1", text1).replace("%2", text2);
}

[[ClientRemoteCall]]
void ReceiveInfo(string text, TextPackKey textKey, string textArgs)
{
    if (text.isEmpty()) {
        Info(MessageBoxType::Default, textKey, textArgs);
    }
    else {
        Info(MessageBoxType::Default, text);
    }
}

[[ClientRemoteCall]]
void ReceiveSay(ident crId, SayType sayType, bool onHeadOnly, string text, TextPackKey textKey, string textArgs)
{
    Critter? cr = Game.GetCritter(crId);

    LocalSay(cr, sayType, onHeadOnly, text, textKey, textArgs);
}

#endif

}
