namespace Wanted
{

#if SERVER

// Author: cvet

bool WantedSignSet(Item wantedSign, string name, int cost)
{
    if (wantedSign.Info != 2) {
        wantedSign.Info = 2;
    }
    wantedSign.TextArgs = TextFormatting::TextArg("name", name) + "|" + TextFormatting::TextArg("cost", cost);
    return true;
}

#endif

}
