// NcrDuppo — серверная инициализация торговца Duppo (НКР).
// Обязанности: делегирование инициализации в Trader::TraderInit. Сторона: SERVER.
// Связи: Trader.
namespace NcrDuppo
{

#if SERVER

// Инициализация Duppo: вся торговая логика — общая, поэтому делегируем в Trader.
void DuppoInit(Critter dappo, bool firstTime)
{
    Trader::TraderInit(dappo, firstTime);
    // караваны будут новые
}

#endif

}
