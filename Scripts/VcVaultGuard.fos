// VcVaultGuard — серверная охрана убежища Vault City: реакция на появление игрока внутри.
// Сторона SERVER (#if SERVER). VaultGuardInit — инициализация криттера-стража при спавне,
// InVault — обработчик ItemTrigger на статичном триггере входа в само убежище.
// Обе функции пока пустые заглушки: поведение стража ещё не реализовано.
namespace VcVaultGuard
{

#if SERVER

void VaultGuardInit(Critter cr, bool firstTime)
{
}

// срабатывает, когда игрок заходит в само убежище
[[ItemTrigger]]
void InVault(Critter player, StaticItem trigger, bool entered, mdir dir)
{
}

#endif

}
