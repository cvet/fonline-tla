// FOS Server
// Author: cvet

// #include "Tla"

#define MODULE_NAME "den_triggers"
#define STR_MSG     (1400)

void t_BarBekkyBoys(Critter cr, const Item trigger, bool entered, uint dir)
{
    if (entered && Random(0, 50) == 0)
        cr.SayMsg(SAY_NETMSG, TEXTMSG_TEXT, STR_MSG);
}
