namespace MoveRandom
{

#if SERVER

bool CritterMoveRandom(Critter cr)
{
    if (cr != null && !cr.ControlledByPlayer) {
        Map map = cr.GetMap();
        if (map != null) {
            int tries = 6;

            for (int i = 0; i < tries; i++) {
                mpos hex = cr.Hex;
                uint8 dir = Game.Random(0, 5);
                map.MoveHexByDir(hex, dir, 1);
                if (map.IsHexMovable(hex)) {
                    NpcPlanes::AddWalkPlane(cr, AI_PLANE_WALK_PRIORITY, hex, dir, false, 0);
                    return true;
                }
            }
        }
    }

    return false;
}

#endif

}
