namespace DenTriggers
{

#if SERVER

// Author: cvet

#define MODULE_NAME "den_triggers"
#define STR_MSG     (1400)

void t_BarBekkyBoys(Critter cr, StaticItem trigger, bool entered, int dir)
{
    if (entered && Game.Random(0, 50) == 0) {
        Messaging::Info(cr, TextPackName::Text, STR_MSG);
    }
}

#endif

}
